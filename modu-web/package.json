{
  "name": "modu-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "docs": "node scripts/build-docs-pages.mjs",
    "build": "npm run docs && vite build",
    "check": "npm run build && node scripts/check-site.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name modu-web",
    "deploy:preview": "npm run build && npx wrangler pages deploy dist --project-name modu-web --branch preview"
  },
  "devDependencies": {
    "vite": "^6.4.2"
  },
  "overrides": {
    "postcss": ">=8.5.23",
    "esbuild": ">=0.28.1"
  }
}
